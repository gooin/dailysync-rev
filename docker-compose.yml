services:
  daily-sync:
    image: 'daily-sync:latest'
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - .env
    command: yarn migrate_garmin_cn_to_global
    volumes:
      - ./db:/app/db
      - ./log:/app/log
    container_name: daily-sync
    tty: true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"

  export-garmin-sessions:
    image: 'daily-sync:latest'
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./db:/app/db
    profiles:
      - tools
    command: yarn export_garmin_sessions
